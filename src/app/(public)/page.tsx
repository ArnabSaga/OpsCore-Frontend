import { Button } from "@/components/ui/button";

const HomePage = () => {
  return (
    <div>
      <h1>OpsCore</h1>
      <p>
        A platform where different businesses can sign up, create their own private workspace,
        invite team members, manage products, customers, orders, analytics, and business automation
        — all inside one shared SaaS platform with strict tenant data isolation.
      </p>
      <Button>Click Me</Button>
    </div>
  );
};

export default HomePage;
